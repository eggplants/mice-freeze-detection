import cv2
import numpy as np
from PySide6.QtWidgets import QWidget
from typing import Any, Optional

class DetectedWidget(QWidget):
    video_path: Any = ...
    data: Any = ...
    raw_video: Any = ...
    raw_video_frames: Any = ...
    processed_video_frames: Any = ...
    threshold: int = ...
    def __init__(self, video_path: str, data: list[int], raw_video: cv2.VideoCapture, processed_video_frames: list[np.ndarray], parent: Optional[Any]=...) -> None: ...
    def convert_boolean_with_threshold(self) -> list[int]: ...
    def show(self) -> None: ...
